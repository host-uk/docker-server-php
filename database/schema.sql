/*
  To examine a data backup:
  1. Paste your SQL dump content into this file.
  2. Run `make reset-db` to destroy the existing database volume and restart the containers.
  3. The database will initialize with the data from this file.
*/

/* Add your SQL schema definitions here, last export, fireup the dev server and inspect the data dump */
